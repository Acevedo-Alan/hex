spring:
  application:
    name: hex-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/hexa_db
    username: alan
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

cloudinary:
  cloud-name: vngrvoiz
  api-key: 749569668139118
  api-secret: gh92FW5Ic5cLBG5OoB58caWBaKg